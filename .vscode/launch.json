{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "erlang",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "Compile",
            "arguments": "-eval \"application:ensure_all_started(erlvscode)\""
        }
    ]
}
